# Copy to .env on the VPS. .env is gitignored — never commit real secrets.
GRAFANA_ADMIN_PASSWORD=change-me
GRAFANA_ROOT_URL=https://grafana.example.bg

POSTGRES_DB=viridian
POSTGRES_USER=viridian
POSTGRES_PASSWORD=change-me

# Alertmanager notification target (internal, not customer-facing)
ALERT_EMAIL_TO=alerts@example.bg
